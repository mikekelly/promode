# Task 31 — Reconstruct rounds 2+3: reference-conformance doctrine (T23–T29) + anti-slop (PD12)

## Brief
- **Orient** — spec: `tasks/30-design-thinking-recovery.md` (§Round 2 and §Round 3 of the recovery record). Read the register (`plugins/promode/docs/opinion-register.md`), the wiki (`plugins/promode/docs/agent-knowledge-wiki.md`), the two docs being superseded (`plugins/promode/docs/design-system-lookbook.md`, `plugins/promode/docs/live-reload-server.md`), the brief (`plugins/promode/PROMODE_MAIN_AGENT.md` §test-strategy), and the affected defs (`plugins/promode/agents/{senior-product-designer,verifier,environment-manager,auditor,chief-product-officer}.md`).
- **Specify** — two commits, one per round:
  1. **Round-2 commit**: new routed doc `plugins/promode/docs/reference-conformance.md` (incl. the DESIGN.md recognise-and-demote ecosystem note); register rows T23–T29 with T21 + DOC-lookbook superseded in place (strikethrough/`~~…~~` retired-row convention as used for PDE/T21 precedents — superseded, never silently deleted, M3); both old docs deleted from live routing but their rules inventoried (O16: every rule the cut prose carried shown surviving in a named home, or recorded as deliberately retired-with-why — the record names two corrections: live-refresh-into-verification wiring retired, 8-section DESIGN.md structure retired as tutorial-grade); per-home edits across brief §test-strategy + SPD/VER/AUD/CPO/EM defs (VER and EM gain proper calibrated homes they never had; AUD gains a conditional "Design references & visual conformance" dimension; T24 homes column includes B); decision node `docs/decisions/2026-07-reference-conformance.md` with the six recorded rejected alternatives (incl. lookbook-as-fallback — curated screenshots survive inside the mirror; in-repo-only truth — the mirror already is the in-repo truth) + reconstruction-provenance note.
  2. **Round-3 commit**: register row PD12 `decisions-not-defaults` (PD2's generative twin clause + unchosen-minimalism clause travel in the statement); SPD `<decisions-not-defaults>` authoring block + red flag ("defended as 'that's what it generated'" — generation is not a decision) + VOCABULARY.md owns dated per-project banned machine-voice patterns (copy tells ride T29 — no new row); auditor refcon-check items 5–6 (incl. "are the references themselves decided?"); refcon frontend-design boundary rewritten around decided-vs-default triage with the kill-ai-slop catalog cited in exactly ONE live home (refcon doc; unlicensed — read, never vendor; time-bound orientation); decision node `docs/decisions/2026-07-anti-slop-pd12.md` with the seven recorded rejected alternatives (vendoring; skill form; standing "de-slop pass" agent — would institutionalise post-hoc scrubbing when the point is slop never entering the reference; relying on frontend-design alone; upstream-scanner recommendation — no license/2 days old/Tailwind-specific; …) + provenance note. No brief edit (verified in the original: PD9 routing already covers).
  Success: all recorded facts honoured; `scripts/check-hooks.sh` and `scripts/check-claude-md-imports.sh` green; every hook chunk under 10,000 chars; no tell-catalog text in any def (M1); K5 one-home citation; register Components table updated (DOC-lookbook superseded → DOC-refcon entry).
- **Why** — these decisions were owner-ratified and fresh-review APPROVED in the lost session; only their prose was lost. Re-litigating them would overwrite ratified owner preference (M0).
- **Verified vs assumed** — the recorded facts are verified transcript quotes; exact prose and the precise 15-edit enumeration are lost — compose in corpus style, deriving the home set coherently from the register rows you write. Do not import content from overlay-mono itself; the record is the spec.
- **Not / exit** — no new judgement calls beyond prose composition; do not touch engineer/worker shared bodies (checksum families); do not recreate the lost branch's task docs. Exit: both commits landed, guards green, Outcome recorded here.

## State
- **Established facts** — corpus at pre-session state; T21 at register line ~130, DOC-lookbook in Components, SPD row cites T21/DOC-lookbook.
- **Open constraints** — provenance note (see task 30); M3 supersession; M1; K5.
- **Pending goals / next step** — task 32 chains on this.

## Outcome
(filled by the agent on completion)
