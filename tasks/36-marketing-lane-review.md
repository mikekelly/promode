# Review: the whole marketing lane

## Brief
- **Orient** — the full diff of tasks 31–35 on this branch (defs, routed docs, register, decision node, brief, README, auditor).
- **Specify** — fresh-spawn `promode:code-reviewer` (`model: opus`) over the lane as one review surface. Axes: (1) spec — every ratified MK item has exactly the homes the register claims, every new def clause serves a register item (the two-way fix rule), the CPO carve left no dangling or duplicated remit, M2/M3 duplications carry their why in every home; (2) standards — house def structure, register table format, K5 one-idea-one-home across the new routed docs, tier-3 items carry verified-as-of + re-verify triggers, checksum families (engineer/worker bodies, behavioural-authority, reporting block) untouched, `check-hooks.sh` + `check-claude-md-imports.sh` green.
- **Why** — the lane is roster + register surgery landed across six tasks by different agents; one unprimed pass over the merged surface catches the cross-task drift per-task reviews can't (O10 — the reviewer gets this doc's checklist of *what to examine*, never expected conclusions).
- **Not / exit** — reviewer judges by reading; REWORK findings route back through the main agent to the owning task's lane. Exit: APPROVED, or REWORK with cited findings, recorded in Outcome.

## State
- **Established facts** — chained on tasks 34 AND 35 (the last writers).

## Outcome

**Verdict: REWORK** (2026-07-16, code-reviewer, opus). The lane is substantively excellent — coherent across six tasks, defs tight, two-way-fix clean, Decision A honored, CPO carve clean, checksum families + invariant scripts green. Three documentation-consistency findings, all surgical, none breaking agent behaviour but all violating the register's own newly-minted rules:

1. **[standards, systematic] `⌛` market-pinned glyph is never used in the routed docs; `⚙` is used in its place — corpus-wide.** Task 33/34 introduced `⌛` (market-pinned: re-verify on a platform change) as a *distinct glyph from `⚙`* (harness-pinned: re-verify on a Claude Code change), with the stated rationale that "a reader auditing harness currency must not have to wade through platform tactics." But all ten `marketing-*.md` docs tag their platform-decay claims with `⚙` (marketing-creative-and-copy.md:38,40,42,43; marketing-ai-search.md:20,29; marketing-outbound.md:15,20,24; marketing-distribution.md:16,29; marketing-planning.md:25,26,27; marketing-council.md:18), and `⌛` appears in **zero** docs. This defeats the exact purpose of the split: a corpus-wide grep for `⚙` (harness-currency audit after a Claude Code upgrade) now surfaces ~15 marketing platform tactics that have nothing to do with the harness, and AUD's new marketing-traceability currency check keys off `⌛`-marked sections that don't exist in the docs. marketing-creative-and-copy.md:38 even glosses `⚙` as "tool/model-behaviour claims ... are dated" — the precise meaning `⌛` was created to carry. Fix is mechanical: `⚙`→`⌛` in the marketing docs (incl. the council advisor-position-drift line, which is market/world-pinned, not harness-pinned).

2. **[spec, register accuracy] `mk-search-harvests-demand` overclaims an SM inline home.** The register homes column lists `CMO c (channel strategy) + SM c (exec) + mkt-paid`, and the "Documented calibrations" note enumerates it as one of **five** dual-homed MK items "each carry[ing] ... a calibrated execution copy in SM `<house-doctrine>`." But SM's `<house-doctrine>` carries only four (net-cash, diagnose-before-distributing, organic-before-paid, kill-criteria) — there is no search-harvests stance in the SM def; it lives only in CMO `<marketing-doctrine>` + marketing-paid-ads.md `<channel-selection>`. Homes must match reality (task axis 1). Fix either way: add the one-line stance to SM `<house-doctrine>`, or correct the homes column to `CMO c + mkt-paid` and drop it from the five-item calibration enumeration (make it four). The opinion itself is fully covered — this is a register-accuracy drift, not a coverage gap, and the calibrations note is exactly where the RB sync process would be misled.

3. (Subsumed into #1 — the council `⚙` on advisor-drift is the same glyph error.)

**No blocking issues on:** clause↔register traceability (every CMO/SM clause serves a register item), K5 one-idea-one-home across the ten docs (decision↑/mechanics↓ split is intact and declared), tier-3 verified-as-of + re-verify triggers (all present and dated 2026-07-15), CPO carve (no dangling positioning/growth remit; boundary text bidirectional), checksum families (engineer/worker/reporting/behavioural-authority all byte-identical per check-hooks.sh), `check-claude-md-imports.sh` green, counts (19 defs, 10 docs) match register claims. kin-as-extensions (5 flags) correctly resolved into existing rows, not rival slugs.

Route #1 back to the task-32/34 doc lane (docs + register glyph convention), #2 to task-33 lane (register/SM def). Both mechanical.
