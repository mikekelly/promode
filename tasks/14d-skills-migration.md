# Migration D — register/README/CLAUDE.md/wiki/runbook sweep

## Brief
- **Orient** — the CANONICAL spec is `tasks/13-skills-elimination-design.md` → Outcome → per-skill disposition table + "Delegation plan" (your task letter). Read it in full before acting; it carries the maintainer-ratified dispositions, the five harness-pinned probe facts (⚙), and per-task scope/checkpoints. Owner/mode: after B+C.
- **Specify** — execute exactly your lettered task from the plan; deliverable, exclusions, and exit condition as stated there.
- **Why** — ratified opinion (2026-07-07): promode avoids voluntary-invocation skills; capabilities move to the four non-voluntary surfaces (dedicated agent / def prompting / def-directed doc read / commands), preserving JIT economics.
- **Verified vs assumed** — probe facts P1–P5 verified live 2.1.201 (see 13); re-verify file locations by reading before editing.
- **Not / exit** — stay inside your letter's scope (other letters own adjacent files); `./scripts/check-hooks.sh` green at your exit (except where your letter says the checkpoint owns it); commit; Outcome recorded in this doc (worktree agents: your tracked copy — it merges); no bump (final task bumps), no push.

## State (Active-State Index)
- **Unresolved errors** — none
- **Open constraints** — per 13's plan
- **Established facts** — ratification 2026-07-07; probes P1–P5
- **Pending goals / next step** — execute per 13

## Outcome
_(senior-engineer, 2026-07-07 — executed; single commit on this branch, bumped 2.36.0 (final task), no push)_

**Done, per 13 §5 + the dispatch brief:**
1. **Register** (`plugins/promode/docs/opinion-register.md`) — new **M5 `no-voluntary-invocation`** row (four-surface matrix as the operational form; determinism-over-listing-voluntariness why; `w:` → the new decision node). Header re-scoped (brief + 11 defs + commands + routed docs); home keys gained AUD/CRF, routed-doc keys (d2d/lookbook now full paths), CMD-<name>; markers line def/doc/command. Row re-homings: O7 (→AA mechanics + inspector path), O8/O15 (→B§task-docs), O19 (→B§planning+§task-docs), O11/O36 (→AUD), P7/T12/T17 (→d2d doc; T17 keeps `w:d2d`, FW noted as inline mirror), K6 (→CRF), T21 (→lookbook), O32 statement ("skill sections"→"brief/def sections"); V6 untouched (harness built-in). Components: intro reframed (no skills to list, per M5), AG-agent-analyzer coordination updated (recovery dispatch + inspector fold), **AG-auditor + AG-constraint-reinforcer** rows added, Skills subsection replaced by **Commands** (CMD-handoff — proactive-trigger why for staying model-invocable; CMD-promode-audit — thin shim) and **Docs (routed mechanics)** (DOC-d2d, DOC-lookbook — consuming defs cited). Harness-pinned ⚙ section gained the five **probe facts P1–P5** (labelled "probe P1…P5" to avoid colliding with P-row ids), verified 2.1.201 / 2026-07-07, citing tasks/13 + the decision node.
2. **README** — intro line ("a couple of slash commands"); agents table +2 rows (auditor, constraint-reinforcer); §Skills → **"No skills — by design"** (the four-surface story, decision-node link); Fork-it §3 + homes parenthetical component-ised. Requirements/Install untouched (not stale).
3. **Root CLAUDE.md** (49 lines, cap held) — What-is bullet list: Skills bullet → Commands + no-skills bullet; altitude paragraph → def / routed doc / command (d2d + lookbook examples reworded); brief design-constraint bullet ("definitions, routed docs, and commands"); the `plugins/promode/skills/` line → commands + docs dirs; decisions line extended to link `2026-07-skills-elimination.md`.
4. **Wiki** — "Authoring skills (SKILL.md) — and promode's routed mechanics docs": M5 stance stated (promode ships none; target repos may; auditor's lens audits *theirs*), same conventions now explicitly govern routed docs + command bodies; bullets kept (last one generalised).
5. **Decision node** — `docs/decisions/2026-07-skills-elimination.md` created (the brief header's dangling citation now resolves): decided/why, four-surface matrix, probe evidence pointer, rejected alternatives with durable whys (disable-model-invocation, d2d-as-agent, audit-synthesis-stays-main, + brief-only and docs-only in short), reversibility. Linked from CLAUDE.md's decisions line.
6. **Runbooks** — `verify-hook-delivery.md` gained the **Live probe** section (✓1 mechanics: `--plugin-dir` takes the plugin dir itself → loads as `name@inline`, init event's `plugins` array = deterministic loaded-check; `-p --output-format json` returns an event ARRAY → `jq '.[-1].result'`; hook `systemMessage` invisible headless → interactive-only for the banner, headless asserts the model-facing side). `cut-a-release.md` steps 1/2/5 de-skilled; `sync-a-shared-principle.md` step 4 → auditor def (worked-example line annotated as historical). `add-a-subagent.md` + `RUNBOOKS.md` had nothing stale.
7. **plugin.json** — `skills` keyword → agents/commands; **bumped 2.35.0 → 2.36.0** via `scripts/bump-version.sh`. Also swept `.claude-plugin/marketplace.json` tags (same staleness, caught by the final grep). Plus: `KANBAN_BOARD.md` header ("task-docs skill" → brief's `<task-docs>`), `docs/decisions/2026-07-community-skills-rejections.md` adopted-content parenthetical, `IDEAS.md` inbound-triage idea re-surfaced per M5.

**`./scripts/check-hooks.sh` fully green** (13 components: 11 agents + 2 commands + 0 skills; 4 chunks; version banner matches 2.36.0; imports resolve).

**Grep survivors (`grep -ri "skill"`, all deliberate):** tasks/* + DONE.md — *historical*; KANBAN card titles — *historical*; register M5/probe-P4/Components-intro + README no-skills section + wiki reframe + both decision nodes — *stance*; auditor skill-quality lens + wiki conventions — *target-repo*; VER `/verify` + lookbook/d2d `frontend-design`/`axi`/`managing-agent-knowledge` mentions — *harness-skills*; cut-a-release commit-quote + sync worked-example + main-agent-delivery/managing-promode-retirement — *historical*; scripts' SKILLS_DIR legacy branch + test fixtures — *legacy-support* (task E's design); README "same context economics as a skill" — *comparative*.

**Not verified / assumptions:** the runbook's live-probe section transcribes the ✓1 checkpoint's pinned mechanics from the dispatch brief — not re-probed here; register component coordination rows written from the new defs/commands as read, not re-executed; no live session run (script-level checks only, per brief — ✓2 owns the live install probe).
