# Brief cruft surgery (post-28 skeptical pass, ratified findings)

## Brief
- **Orient** — `plugins/promode/PROMODE_MAIN_AGENT.md` (all edits land here), `plugins/promode/hooks/hooks.json` + `scripts/check-hooks.sh` (chunk/size gates), `plugins/promode/docs/opinion-register.md` (rows O1, O4, O5, O13 — wording syncs in-scope here, narrowly), precedent `tasks/28-brief-concision.md`. Findings ratified by the maintainer 2026-07-16 from a Fable-tier review.
- **Specify** — apply six ratified findings in one pass:
  1. **Rewrite `<background-delegation>` against the current harness.** Established facts, verified against the MAIN agent's live tool schema 2026-07-16 (do not re-derive from a subagent's view): (a) the Agent tool now runs subagents **in the background by default** — its schema says "Subagents run in the background by default; you'll be notified when one completes. Pass `run_in_background: false` for a synchronous run"; so the brief's "(1) call with `run_in_background: true`" protocol instructs opting into the default — rewrite to "delegation is background by default; never pass `run_in_background: false`" + end-the-turn discipline. (b) `TaskOutput` **still exists** (deferred tool, main-agent inventory) — keep "never TaskOutput", rationale intact (the notification already delivers the result). (c) SendMessage steer/resume is now documented in the Agent tool description itself — compress the brief's restatement to the *judgement* the schema doesn't carry: steer/resume-vs-fresh-spawn (O5), planned serial reuse. Keep: never-poll/never-read-`.output` (context protection), worktree conditional (O6), recovery via agent-analyzer (O7). The `"Work delegated as required"` magic phrase: **cut it** (no rationale exists anywhere in the repo — M1/M3 violation as written); replace with plain "tell the user what was delegated, then end the turn".
  2. **Dedupe the C-suite one-way-door list.** Canonical enumeration stays in `<delegation-map>`; `<role>` and `<product-considerations>` reduce to a cross-reference ("the crucial product one-way doors — canonical list in `<delegation-map>`"). Fix the CTO list's role/map duplication the same way opportunistically. Intra-brief cross-refs are NOT pointer-demotion (task 28 precedent); M4 is satisfied because the brief is delivered whole.
  3. **Trim the model-reference table to Tier | Model.** Window/Effort columns serve no brief decision (effort isn't dispatchable, O45; no decision consumes window sizes). **Chained register edit required**: O13's homes cell cites "the model-reference table … windows + effort levels" — sync that wording or the cut reads as drift.
  4. **Trim `<task-docs>` meta-commentary** (~15%): the "This serves *context is precious*…" restatement, the handoff-distinction parenthetical under "Where." (duplicates `<handoff>`), "(its definition baked this in)" in Lifecycle step 3. Template and all O19/O46 content stay.
  5. **Fix `<role>`'s "(a frontier model on high effort)"** — asserts a possibly-false fact on a below-frontier session (O39 contemplates exactly that). Reword to intent (e.g. "reserve your tier for orchestration") or cut; O1's framing must survive.
  6. **Minor, opportunistic:** dedupe `<principles>` bullet 1 vs `<role>`'s delegate-by-default/guard-your-context (keep one framing; register O1 home marker `v` may need honest downgrade to `c` — sync it); cut the AAR runbook example enumeration "(deploy, migration, env bring-up…)" (M1-derivable; K4 survives).
- **O16 inventory required** — every cut line listed in the report with the register item it carried and the surviving home.
- **Why** — a Fable-tier skeptical pass post-task-28 found the harness moved under `<background-delegation>` (⚙ facts dated 2.1.201/202) and three duplication/over-prescription classes; over-prescription degrades strong models (O34/M1). XML tags were reviewed and **kept** (Anthropic July-2026 guidance still endorses them; `B§` anchors + checksum tooling depend on the convention) — do not touch the tag structure.
- **Verified vs assumed** — facts (a)–(c) in item 1 verified against the main agent's schema 2026-07-16 (this is the authoritative view — the brief is delivered to the main agent). Assumed: chunk sizes will shrink, not grow; if any chunk approaches 10k, rebalance at section boundaries and keep `hooks.json` registration in sync (M4).
- **Not / exit** — do NOT restructure sections beyond the findings, rename tags, or touch agent defs; register edits ONLY the two wording syncs named above (O13, O1 markers). Exit: brief + register wording committed, `scripts/check-hooks.sh` green (state the chunk sizes in the report), O16 inventory in report + Outcome.

## State
- **Established facts** — XML tag convention: KEEP (ratified 2026-07-16). Marketing-lane task 34 will later edit the brief's `<delegation-map>`/`<product-considerations>` — this task lands FIRST so 34 rebases on the deduped text.
- **Open constraints** — worktree-isolated agents record the Outcome in their own worktree's copy.

## Outcome
All six ratified findings applied to `plugins/promode/PROMODE_MAIN_AGENT.md`; two named register wording syncs applied to `plugins/promode/docs/opinion-register.md`. `scripts/check-hooks.sh` green before and after. No section restructuring, no tag renames, no agent-def edits, no register edits beyond O13 + O1.

**Chunk sizes (delivered, main-agent path) — baseline → after:** c1 9210→8800, c2 6997→6825, c3 6661→6661, c4 8436→8375, c5 8722→8369. All shrank or held; none grew, none near the 10k cap (assumption in the brief held).

**Findings applied:**
1. `<background-delegation>` rewritten against the current harness: protocol step (1) now "runs in the background by default; never pass `run_in_background: false`" (foreground prohibition + its rationale folded in, absorbing the old standalone "NEVER run in the foreground" bullet); "never TaskOutput" kept with rationale; `SendMessage` restatement compressed to the judgement the Agent-tool schema doesn't carry (steer/resume-vs-fresh-spawn O5, planned serial reuse, running-agent queue timing); never-poll/never-read-`.output`, worktree conditional (O6), recovery (O7) all kept.
2. C-suite one-way-door enumeration deduped: `<delegation-map>` stays canonical; `<role>` and `<product-considerations>` reduced to cross-references; CTO role/map duplication fixed in `<role>` the same way.
3. Model-reference table trimmed to Tier | Model (Window + Effort columns cut).
4. `<task-docs>` meta-commentary trimmed: "This serves *context is precious*…" sentence, the handoff-distinction parenthetical under "Where.", and "(its definition baked this in)" in Lifecycle step 3. Template + O19/O46 content untouched.
5. `<role>`'s "(a frontier model on high effort)" cut; O1's framing (delegate-by-default, guard-your-context, remedy-is-delegation) survives intact.
6. `<principles>` bullet 1 ("Context is precious — delegate by default") cut as a duplicate of `<role>`; AAR runbook example enumeration "(deploy, migration, env bring-up…)" cut.

**Register syncs:** O13 homes cell — "windows + effort levels" dropped from the model-reference-table citation. O1 homes cell — B§principles home removed and `v` marker dropped (was "B§role, B§principles v" → "B§role"), since the duplicated principles bullet was cut, leaving a single home (honest reflection rather than a `v`→`c` downgrade, which would have required two surviving near-duplicate homes).

**O16 cut-inventory (each cut line → register item it carried → surviving home):**
- `"Work delegated as required"` magic phrase → O4 (fire-and-forget end-the-turn) → surviving home: `<background-delegation>` step (2)+(3) ("tell the user what you delegated, then end your turn"). The phrase itself carried no rationale anywhere in the repo (M1/M3 violation as written) — cut, not relocated.
- `<background-delegation>` "NEVER run an agent in the foreground — it blocks you, kills parallelism, and loses mid-flight steering" bullet → O4 ("never foreground") → surviving home: same section, step (1) parenthetical ("a synchronous run blocks you, kills parallelism, and loses mid-flight steering") — rationale preserved verbatim (M3).
- `<background-delegation>` SendMessage mechanics (running-vs-completed resume detail, `agentId` from spawn result, "recalls its brief verbatim", "same task-id may notify more than once", "re-wake is session-scoped; transcripts persist") → O5 (steer/resume-vs-fresh-spawn) → the mechanic now lives in the Agent tool's own description (schema-documented); the judgement (O5's actual register statement) survives in the compressed bullet. "re-wake is session-scoped; transcripts persist" is independently restated in `<after-action-review>`.
- `<role>` "(a frontier model on high effort)" → no register item (a possibly-false self-fact O39 contemplates) → cut, nothing to preserve; O1 framing survives in `<role>`.
- `<role>` / `<product-considerations>` C-suite one-way-door enumerations → O2 (main-agent-ratifies-cto-drafts) / PD9 (pde-consult-routing) → canonical enumeration survives in `<delegation-map>` (product line + CTO line); both sections cross-reference it. O2/PD9's ratification-and-routing decision text survives in both sections (only the list moved).
- `<role>` CTO scope enumeration ("architecture, the entity/domain model, large-refactor strategy") → O2 / A2 → surviving home: `<delegation-map>` CTO line.
- Model table Window column (1M / 200K) → no register item (no brief decision consumes window sizes) → cut, nothing to preserve.
- Model table Effort column (low/med/high/… / none) → O13 (model-effort-ladder) + O45 (effort-pinned-per-def) → surviving homes: `<model-tiers>` prose ("Haiku has no effort control"; the O45 "never pass effort per-dispatch" paragraph) already carries every effort fact; O13 register citation synced.
- `<principles>` bullet 1 "Context is precious — delegate by default" → O1 (delegate-by-default) → surviving home: `<role>` ("Delegate by default" + "Guard your context" + remedy-is-delegation). "context is precious" phrase itself still appears in the Traceable-by-construction bullet.
- `<task-docs>` "This serves *context is precious*: the plan state lives on disk, not in your window." → O46/O19 restatement of context-is-precious → surviving home: `<role>` (context-is-precious as O1) + the section's own "so the brief, decisions, and outcome live in the repo" opener; no unique rule lost.
- `<task-docs>` handoff-distinction parenthetical → task-doc-vs-handoff distinction → surviving home: `<handoff>` ("durable task state belongs in task docs").
- `<task-docs>` "(its definition baked this in)" → K1/K2/O19 (agents record Outcome per their defs) → surviving home: the worker/engineer defs' `<reporting>`/Outcome discipline; meta-note redundant in the brief.
- `<after-action-review>` runbook example list "(deploy, migration, env bring-up, recovery, a recurring incident class)" → K4 (runbooks-prefer-scripts) → surviving home: K4 rule itself, unchanged ("a repeatable operational procedure → a runbook linked from a `RUNBOOKS.md` hub… prefer a script").

**Not verified / assumptions:** Facts (a)–(c) in finding 1 were taken as given from the task doc (verified against the MAIN agent's live schema 2026-07-16 by the maintainer); I did not independently re-probe the harness — but the Agent tool schema visible in *this* session's own tooling corroborates (a) "Subagents run in the background by default… Pass `run_in_background: false` for a synchronous run" and (c) "Use SendMessage… to continue a previously spawned agent with its context intact". I did not render the full delivered chunk text to eyeball merge boundaries beyond the size/registration gate. O4's register statement ("Always `run_in_background: true`… never foreground") was left untouched per the NOT-do (register edits limited to O13+O1); it remains semantically equivalent to the new "never pass `run_in_background: false`" wording, so not drift — but a future pass may want to sync O4's wording for literal accuracy.
