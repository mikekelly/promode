# Product split: chief-product-officer (inherit) + senior-product-designer (opus/high)

## Brief
- **Orient** — `plugins/promode/agents/product-design-expert.md` (the def being split; read fully) and `plugins/promode/agents/chief-technology-officer.md` (the contract template the CPO mirrors: draft-for-ratification, recommendation + strongest rejected alternatives, inherit pin + its rationale). Register rows PD1–PD11, A1, K8, R6, O2 in `plugins/promode/docs/opinion-register.md`.
- **Specify** — split PDE into two defs; use `git mv product-design-expert.md senior-product-designer.md` so history follows the larger heir, then create `chief-product-officer.md`:
  - **`chief-product-officer.md`** (new): `model: inherit`, NO effort field; description mirrors CTO's ("inherits the session's top model…" — cost-ceiling rationale included). Remit — drafts the crucial, hard-to-reverse **product** calls for main-agent ratification: goal-hierarchy changes (PD5/PD6 — the top of the hierarchy clears a higher bar and now has an owner at the right tier), persona establishment/major revision (PD3), positioning, growth strategy, kill/build calls. Owns the **claim side** of PD4/A1: user needs as evidence-bearing claims, evidence grading, never fabricated — an unvalidated user-need assumption propagates into the entity model, the costliest layer to unwind (that why must travel). Carries the skeptical core (PD1 default-no, PD6 post-hoc-justification trap) calibrated to decision altitude. Contract mirrors CTO: recommendation + strongest rejected alternatives + delegation-ready follow-ups; decision-node discipline (K3); escalation. Boundary text both directions: entity/domain model + deep technical trade-offs → CTO (both-draft-in-parallel-unprimed when genuinely both, main agent synthesises); execution-level product work → senior-product-designer.
  - **`senior-product-designer.md`** (renamed from PDE): `model: opus`, `effort: high` (PDE's current "defaults to Opus" becomes the explicit pin), description updated. Keeps PDE's execution mechanics: PD9 consults, PD2 defaults-over-settings, PD10 UI-variant spikes, PD11 holistic lenses, T21 lookbook conditional read, K8 ownership of `docs/product/` incl. PERSONAS.md maintenance, R6 Approve/Refine/Reject verdicts, skeptical core (PD1/PD4/PD6) calibrated to execution altitude. Rewrite its crucial-calls routing: hard-to-reverse product calls now route to `chief-product-officer` (the old "most crucial product-technical calls go to chief-technology-officer" line narrows to the technical-trade-off case).
  - **Cut/moved-prose inventory (O16 — required in report):** every PDE clause shown surviving in CPO, SPD, or named as deliberately retired with why.
- **Why** — PD4/A1 call unvalidated user-need assumptions the costliest mistake, yet their guardian sat a tier below the entity model's owner; the CPO gives crucial product one-way doors the same inherit-tier draft-for-ratification treatment as technical ones (O2 symmetry). Ratified 2026-07-10.
- **Verified vs assumed** — `model: inherit` valid (probed ⚙). Assumed: the decision/execution altitude split of PD opinions is clean; where a PD opinion genuinely belongs in BOTH homes, duplicate it with the why (M2/M3) rather than choosing — flag such duplications in the report for the register sync (task 24) to record.
- **Not / exit** — do NOT edit the register, brief, README, CTO def (task 25 adjusts its boundary line), or other defs. Exit: both files committed (rename + new), inventory in report + Outcome.

## State
- **Established facts** — frontmatter ratified and exact. CPO must NEVER be `model: fable` (cost ceiling — task 19's node).
- **Open constraints** — worktree-isolated: Outcome goes in YOUR worktree's copy of this doc.

## Outcome
**Done (2026-07-10).** PDE split into two defs, both committed.

- `git mv product-design-expert.md senior-product-designer.md` (history follows the heir). Frontmatter → `name: senior-product-designer`, `model: opus`, `effort: high` (the old "defaults to Opus" now an explicit pin), description rewritten (execution tier; crucial calls → CPO, deep-technical-trade-off calls → CTO). Body: `<your-role>` opening rewritten to execution altitude + an "Altitude — you execute, you don't draft the one-way doors" paragraph routing crucial product one-way doors to `chief-product-officer` and deep-technical-trade-off product calls to CTO; `<escalation>` gained a route-up trigger for those calls. All execution mechanics retained verbatim (see inventory).
- `chief-product-officer.md` (new): `model: inherit`, NO effort field. Contract mirrors CTO — `<reporting>` (recommendation + strongest rejected alternatives + decision log + lead-with-tweakables + assumptions line + task-doc record), `<your-role>` (inherit + cost-ceiling rationale, draft-for-ratification, skeptical-core-sharpened-at-altitude with PD6 post-hoc trap, PD4/A1 claim-side ownership, decide-others-execute), `<boundaries>` (both directions incl. parallel-unprimed-draft with CTO for genuinely-joint calls), `<decision-nodes>` (K3), `<escalation>`.

**Duplications to flag for register sync (task 24), per M2/M3 (both homes, why travels):**
1. **PD4/A1 claim-side** (user needs as graded evidence-bearing claims; unvalidated need propagates into the entity model, costliest to unwind): fullest copy now in CPO `<your-role>` at decision altitude; a calibrated execution-altitude copy retained in SPD `<lenses>` ("Why getting the user need right…"). Genuinely both — do not collapse.
2. **PD1 skeptical default / PD6 post-hoc-justification trap**: retained in SPD (`<how-you-think>`, `<red-flags>`) at execution altitude; a decision-altitude copy added to CPO `<your-role>` ("default answer is no… defend the goal, not the feature"). Both homes.
3. **K3 decision-node discipline + capture rule**: SPD `<agent-knowledge>` (unchanged); CPO `<decision-nodes>` (CTO-style, decision-altitude). Both.

**Register/brief/README/CTO/other defs untouched** (tasks 24/25). `product-design-expert` is now dangling in the register `<delegation-map>`, README table, brief, and register rows — expected, out of my scope.
