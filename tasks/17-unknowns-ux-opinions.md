# Unknowns-UX opinions: blind-spot pass, comprehension gate, plans-lead-with-tweakables

## Brief
- **Orient** — `plugins/promode/docs/opinion-register.md` (O-series ends at O39; Open-questions section carries the parked R4 widening), `plugins/promode/PROMODE_MAIN_AGENT.md` (`<clarifying-outcomes>`, `<execution>`, `<planning>`, `<workflow>`), `plugins/promode/agents/chief-technology-officer.md`, README `## The opinions`, `runbooks/sync-a-shared-principle.md`, `scripts/check-hooks.sh`. Source material: "A Field Guide to Fable: Finding Your Unknowns" (article text in the ratifying conversation; also dzhng's `explore-unknowns` skill, prompting reference only — M5 forbids the skill form).
- **Specify** — maintainer-ratified opinion additions (2026-07-07), framed as **main-agent↔user interaction UX**: techniques the main agent offers the user to drain the *user's* unknowns before, during, and after delegated work. Deliverables, one commit:
  1. **Register: three new O-rows** (statements ratified in substance, polish wording in register voice):
     - **O40 `blind-spot-pass`** — When work enters territory the user signals — or you infer — is unfamiliar to them, run a blind-spot pass before pinning outcomes: delegate exploration to surface the unknown unknowns, then teach the user enough to react. Why: the interview (O18) only drains unknowns someone already perceives; a user who doesn't know what good looks like can't accept criteria for it. Homes: B§clarifying-outcomes.
     - **O41 `user-comprehension-gate`** — A two-ended contract around autonomous runs. **At dispatch/plan time**: set the autonomy remit with the user — how autonomous this run is, which decision classes the main agent may settle on best judgement, which must be surfaced. **At the ratification/merge point**: offer an explainer + comprehension check (quiz) covering the *unratified decision risk* the run accumulated — divergence from the ratified plan, consequential/hard-to-reverse decisions made mid-flight that got less exploration than their stakes warranted, and anything that fell outside or strained the agreed remit. Why: ratification without comprehension is rubber-stamping — the user-facing kin of R4 `distrust-frictionless-approval` — and the remit is what makes the trigger computable rather than a vibe. Calibrations (load-bearing, keep all three): an **offer** the human takes or declines, never an agent-enforced gate; the trigger is **unratified decision risk, never volume** — raw diff size, run length, or "the user wasn't watching" must not fire it (that would tax autonomy itself; a long run that held the plan with all decisions pre-ratified earns no quiz); the gate lives **at the ratification point only** — it never pauses or interrupts autonomous execution. Homes: B§execution (gate) — the remit-setting sentence may sit in B§clarifying-outcomes or B§planning if it reads more naturally there (same item, two ends; implementer's call, cite O41 from both spots). Maintainer-ratified revision 2026-07-07 (superseding the earlier delta-vs-attention draft — record the supersession in task 18's decision node, not here).
     - **O42 `plans-lead-with-tweakables`** — Plans and design drafts presented for ratification lead with the decisions the user is most likely to tweak — entity/data model, type interfaces, user-facing flows — and bury the mechanical tail. Why: the ratification artifact is a reaction surface; ordering it for reaction is part of the deliverable (presentation-side kin of A2/A3). Homes: B§planning; CTO c (its drafts are the main ratification artifacts).
  2. **O18 calibration** (statement + B§clarifying-outcomes text): the clarify phase **opens with starting-point disclosure** — where the user is in their thought process, their experience with the problem and this part of the codebase (this is what decides whether O40 fires) — and **invites a reference when articulation stalls**: the best reference is source code (a folder, a library, a site's underlying markup — even in another language), which beats a lossy verbal description.
  3. **Brief edits** carrying the new clauses: `<clarifying-outcomes>` (O18 calibration + O40 sentence — the blind-spot pass may open the section or sit as its own short paragraph; do NOT create a new top-level section unless the chunk balance forces it), `<execution>` (O41 offer, one-two sentences at the end before/alongside the "done" checkpoint text), `<planning>` (O42 sentence).
  4. **CTO def**: O42 calibrated home — drafts delivered for ratification lead with the tweakable decisions, mechanical tail last (fits its existing A3 recommendation-plus-rejected shape).
  5. **README `## The opinions`**: human-map rows for O40–O42.
  6. **Register housekeeping**: Open-questions R4-widening entry gains a note that O41 now answers the *user-facing* side; the CTO-draft consensus-audit side stays parked.
- **Why** — M0: the register is the owner's preference surface; these three are ratified owner preferences extracted from the article. Every new brief clause must cite/serve its register item (CM discipline).
- **Verified vs assumed** — Section tags and line anchors verified 2026-07-07 (brief has 7 chunks, 33,415 chars total — headroom exists but per-chunk cap must be re-verified). Assumed: no existing def text already carries these stances (grep `blind.?spot|quiz|comprehension|unknown unknown` before writing; if a home exists, calibrate it instead of duplicating).
- **Not / exit** — No skill, no command, no routed doc — these are B-resident decisions (mechanics are conversation + a delegated Explore; thin by design). No AUD dimension changes. Do not touch the decision node or IDEAS.md (task 18 owns those). Exit: `./scripts/check-hooks.sh` fully green (all chunks <10k), every new clause traceable to O40/O41/O42/O18, committed, Outcome recorded. No version bump (task 18 carries it). No push.

## State (Active-State Index)
- **Unresolved errors** — none
- **Open constraints** — chunk caps; register-voice wording; ratified substance must survive verbatim-in-substance
- **Established facts** — ratified 2026-07-07 in-session; O-series next free ids O40–O42
- **Pending goals / next step** — awaiting maintainer ratification of the plan, then dispatch to senior-engineer

## Outcome
(filled by the agent on completion)
