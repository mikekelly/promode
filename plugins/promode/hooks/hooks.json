{
  "description": "promode: SessionStart auto-injection is DISABLED in this fork. promode no longer fires in every session; it is activated on demand via the /promode command, which reads PROMODE_MAIN_AGENT.md and adopts the orchestration brief for that session only.",
  "hooks": {
    "SessionStart": []
  }
}
